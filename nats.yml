version: '3'
services:
 app:
  image: ${IMAGE:-nats}
  tty: true
  stdin_open: ${BOOLEAN:-true}
  environment:
   - ${ENV:-environment}=value1
   - ${ENV2:-environment2}=value2
   - ${ENV3:-environment3}=value3
