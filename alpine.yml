version: '3'

services:

  alpine:
    image: ${IMAGE:-alpine}
    stdin_open: true
    tty: true
    environment:
     - ${SOMETHING:-wup}=hoopla
     - ${BURGER:-krabby}=patty
     - ${THEORY:-big}=theory
