version: '3'
services:
  alpine:
    image: ${IMAGE:-alpine}
    stdin_open: true
    tty: true
    environment:
     - ${SOMETHING:-wup}=hoopla
     - sdf=sdf
     - sdf=xcv
    ports:
     - ${PORTS:-85}:80


