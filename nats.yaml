version: '3'
services:
 app:
  image: ${IMAGE:-alpine}
  tty: ${BOOLEAN:-true}
  stdin_open: ${BOOLEAN:-true}
  ports:
   - 80:80
  environment:
   - ${ENV1:-environment1}=deck
   - ${ENV2:-environment2}=ally

