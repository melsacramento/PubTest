version: '3'
services:
 app:
  image: idonotexistnats
  tty: ${BOOLEAN:-false}
  stdin_open: ${BOOLEAN:-false}
  environment:
   - ${ENV1:-environment1}=deck
   - ${ENV2:-environment2}=ally
   - ${ENV3:-environment3}=go

