version: '3'
services:
 app:
  image: ${IMAGE:-nats}
  tty: true
  stdin_open: ${BOOLEAN:-false}
  environment:
   - ${ENV1:-environment1}={{value}}
   - ${ENV2:-environment2}={{value2}}
   - ${ENV3:-environment3}={{value3}}
   

