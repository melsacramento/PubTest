version: '3'
services:
 app:
  image: ${IMAGE:-kkjkj}
  tty: ${BOOLEAN:-false}
  stdin_open: ${BOOLEAN:-false}
  ports:
   - 80:80
  environment:
   - ${ENV:-environment}=deck
   - ${ENV2:-environment2}=ally
   - ${ENV3:-environment3}=shark