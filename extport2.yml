version: '3'
services:
  webapp2:
    image: nats:latest
    ports:
      - "${PORT:-3003}:90"
    tty: true
    stdin_open: true
    environment:
      - ${TEXT:-wuba}=labadabdab
      - ${SAME:-bag}=labrador
