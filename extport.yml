version: '3.1'
services:
  webapp:
    image: nginx:latest
    ports:
      - "${PORTS:-3001}:91"
    tty: true
    stdin_open: true
    environment:
      - ${PINE:-laba}=dabdab
      - ${SAME:-lambda}=pug
