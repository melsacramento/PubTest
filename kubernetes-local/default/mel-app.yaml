apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mel-app-data
  namespace: default
  labels:
    app: mel-app
    managed-by: portainer-run
  annotations:
    portainer-run/deploy-type: vibe
    portainer-run/git-target-id: 0756625b-fa24-4a6e-bee1-25b9beb9527b
    portainer-run/git-branch: main
    portainer-run/git-path: kubernetes-local/default/mel-app.yaml
    portainer-run/vibe-source-path: kubernetes-local/default/mel-app/src
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mel-app
  namespace: default
  labels:
    app: mel-app
    managed-by: portainer-run
  annotations:
    portainer-run/deploy-type: vibe
    portainer-run/git-target-id: 0756625b-fa24-4a6e-bee1-25b9beb9527b
    portainer-run/git-branch: main
    portainer-run/git-path: kubernetes-local/default/mel-app.yaml
    portainer-run/vibe-source-path: kubernetes-local/default/mel-app/src
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mel-app
  template:
    metadata:
      labels:
        app: mel-app
        managed-by: portainer-run
    spec:
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      automountServiceAccountToken: false
      initContainers:
        - name: vibe-sync
          image: alpine/git:latest
          command:
            - sh
            - '-c'
            - >-
              git config --global http.sslVerify false && git clone --depth 1 --branch main
              https://${GIT_USERNAME}:${GIT_TOKEN}@github.com/melsacramento/PubTest /tmp/repo && mkdir -p
              /usr/share/nginx/html && cp -r /tmp/repo/kubernetes-local/default/mel-app/src/. /usr/share/nginx/html/ &&
              rm -rf /tmp/repo
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 512Mi
          volumeMounts:
            - name: app-data
              mountPath: /usr/share/nginx/html
          env:
            - name: GIT_USERNAME
              valueFrom:
                secretKeyRef:
                  name: mel-app-git-credentials
                  key: username
            - name: GIT_TOKEN
              valueFrom:
                secretKeyRef:
                  name: mel-app-git-credentials
                  key: token
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            seccompProfile:
              type: RuntimeDefault
      containers:
        - name: mel-app
          image: nginxinc/nginx-unprivileged:alpine
          command:
            - sh
            - '-c'
            - nginx -g 'daemon off;'
          workingDir: /usr/share/nginx/html
          ports:
            - containerPort: 8080
              protocol: TCP
          volumeMounts:
            - name: app-data
              mountPath: /usr/share/nginx/html
          resources:
            requests:
              cpu: 100m
              memory: 1Gi
            limits:
              cpu: '1'
              memory: 4Gi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            seccompProfile:
              type: RuntimeDefault
      volumes:
        - name: app-data
          persistentVolumeClaim:
            claimName: mel-app-data
---
apiVersion: v1
kind: Service
metadata:
  name: mel-app
  namespace: default
  labels:
    app: mel-app
    managed-by: portainer-run
  annotations:
    portainer-run/deploy-type: vibe
    portainer-run/git-target-id: 0756625b-fa24-4a6e-bee1-25b9beb9527b
    portainer-run/git-branch: main
    portainer-run/git-path: kubernetes-local/default/mel-app.yaml
    portainer-run/vibe-source-path: kubernetes-local/default/mel-app/src
spec:
  type: ClusterIP
  selector:
    app: mel-app
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
